# Installed with pip inside the DMIDetection conda env (Docker build stage)
# Keep this list focused on packages not explicitly pinned in environment.yml.
# This project expects Python 3.10 (matching environment.yml / PyTorch 1.13 stack).
albumentations==1.3.1
accelerate==0.21.0
diffusers==0.21.4
huggingface_hub==0.25.2
ftfy==6.1.1
imageio==2.31.5
numpy==1.26.4
opencv-python-headless==4.9.0.80
regex==2023.12.25
safetensors==0.4.3
seaborn==0.12.2
tensorboardX==2.6.2.2
tokenizers==0.13.3
transformers==4.33.3
